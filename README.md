# challenge_parser

I used the crawler because it is an algorithm that navigates the web in an automated way, visiting web pages and collecting information, so it was possible to perform the traceability of the sites within the parser domain.